import { TransformTokenInterceptor } from './transform-token.interceptor';

describe('TransformTokenInterceptor', () => {
  it('should be defined', () => {
    expect(new TransformTokenInterceptor()).toBeDefined();
  });
});
